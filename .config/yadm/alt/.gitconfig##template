;;;
; this file is generated edit ~/.config/yadm/alt/.gitconfig##template instead
;;;
[user]
  name = Caleb Cushing
{% if yadm.hostname == "Calebs-MBP" %}
  email = caleb@7pi.co
{% else %}
  email = xenoterracide@gmail.com
{% endif %}
[push]
  default = current
[core]
  autocrlf = input
[alias]
  csa = commit --signoff --all
  fap = fetch --all --prune --prune-tags
  pushf = push --force-with-lease
  mush = merge --squash
  blow = !git branch -d $1 && !git push origin :$1 && :
[rerere]
  enabled = true
[checkout]
  defaultRemote = origin
[pager]
  status = true
[merge]
{% if yadm.os == "Darwin" %}
  tool = meld
  guitool = meld
{% else %}
  tool = vimdiff3
  guitool = vimdiff3
{% endif %}
